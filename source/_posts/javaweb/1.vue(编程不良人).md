---
title: 1.vue
date: 2019-10-06 11:00:00
author: 𝚲𝚳𝚲
tags:   # 添加博文标签
	- javaweb
categories:   # 添加博文分类
	- vue

---

<h1><center>vue</center></h1>



{% raw %}
## 1.vue简介

官方定义：渐进式javascript框架==>简化页面中js操作

通俗定义：js框架 用来简化页面dom操作，通过使用vue实现页面中复杂功能从而不需要频繁操作dom甚至不需要操作任何dom
     				vue框架 前后端分离 基础
特性：MVVM双向绑定机制 Model 数据变化-----视图View

## 2.Vue入门案例

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>备忘录小案例</title>
</head>
<body>
<div id="app">
    <h1>{{msg}}</h1>
</div>
</body>
</html>
<script src="js/vue.js"></script>
<script>
    // 这个Vue对象 V 必须大写！！！！ 
    new Vue({
        el: "#app",
        data: {
            msg: "hello Vue"
        },
        methods: {}
    })
</script>
```



## 总结

>1.一个页面中只能存在一个vue实例
>2.el代表vue实例作用范围日后只有在作用范围内才能使用vue相关语法
>3.data代表vue实例中一系列数据日后只能在vue作用范围内进行数据获取获取数据方式{{属性名}}
>4.在使用{{属性名}}获取数据时获取数据可以进行算数逻辑 调用相关类型相关方法
>5.e1代表vue实例作用范围，el值可以是任何css选择器但是推荐使用id选择器



## 3.Vue指令的一些总结

首先要明确，所有的Vue指令都是写在html的标签内部的，后跟 "="，==v-on=" "== 

​	例如：```<div v-on="fun1">div块元素</div>```

### 3.1  vue指令 

① v-text ,v-html 

​		作用：都是用来获取vue实例中data中声明的属性值，渲染在页面上

​		语法:

​				 例如：```<span v-text="msg"></span>```

​							 ```<span v-html="msg"></span>	```						

v-text、{{}}区别：1.v-text取值覆盖标签原始内容  而 {{}}直接将值插入到指定位置：插值表达式
									 2.v-text在网络条件比较差情况下避免插值闪烁

v-text、v-html		1.v-text直接将获取的数据以文本方式渲染到页面中	相当于 innerText
										2.v-html获取的数据先进行html标签解析解析之后在渲染    innerHTML

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
<div id="app">
    <!--    v-text 和 v-html 的使用
            v-text 相当于 innerText 获取或者插入 父标签 的 子标签插入 text文本
            v-html 相当于 innerHtml 获取或者设置 父标签的 子标签 为 新标签
    -->

    <p v-text="users[1].age"></p>
    <!--    v-text 原样显示 tag内容-->
    <p v-text="tag"></p>
    <!--    v-html 解析tag中的内容，有html标签会解析-->
    <p v-html="tag"></p>
    
</div>
</body>
</html>
<script src="./js/vue.js"></script>
<script>
    var app = new Vue({
        el: "#app",
        data: {
            msg: "这是msg",
            users: [
                {id: 1, name: "黄智贤", age: 23, studentID: 20240901},
                {id: 2, name: "蔡徐坤", age: 11, studentID: 20240902},
                {id: 3, name: "黄晓明", age: 28, studentID: 20240903},
                {id: 4, name: "成龙", age: 53, studentID: 20240904},
            ],
            product: {id: 1, name: "手机", price: 100},
            tag: "<a href='www.baidu.com'>百度一下</a>"
        },
        methods: {}
    });
</script>
```



-----

②.v-on事件绑定
	事件三要素：1.事件源	2.事件	3.监听器
	作用：用来给html标签绑定指定事件
	简化写法：@

​	例如：button标签绑定 单击事件

​				```<button @click="fun"></button>```

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
<div id="app">


    <h1>{{ msg }}</h1>
    <hr>
    <!-- 1.v-on 绑定点击事件 v-on  可简写为 @ -->
    <!--   点击事件 @click="" -->
    <span v-on:click="fun1($event)">测试@click</span>
    <br>
    <br>
    <br>
    <br>
    <!-- 2. @mouseover 鼠标移动到上面 事件  -->
    <span @mouseover="fun2()">测试mouseover</span>
    <br>
    <br>
    <br>
    <br>
    <br>
    <!-- 3. @mouseout鼠标移除事件  -->
    <span @mouseout="fun3()">测试mouseout</span>

    <br>
    <br>
    <br>
    <br>
    <br>
    <!-- 4. @keydown  键盘按下事件 ，并同时获取keyCod -->
    <!--    取event对象 一定使用 $ 来取 -->
    <span @keydown="fun4($event)"><input type="text"></span><br>
    <span>按下的键盘按键的 key_Code是{{ key_code }}</span>
    <br>
    <!--  按键修饰符，和键盘事件连用   -->
    <span @keydown.enter="fun7"><input type="text"></span>
    <br>
    <br>
    <br>
    <span @keydown.ctrl="fun8"><input type="text"></span>
    <br>
    <br>
    <br>
    <br>
    <br>
    <!--    5. -->

    <hr>
    <br>
    <br>
    <br>
    <br>
    <br>
    <br>
    <!--事件传递  子事件，会传递到父标签上  -->
    <div id="div1">

        <div id="div3" style="border: 1px solid rebeccapurple" @mouseover="fun5()">

            <hr>
            <!-- 属性值，不能使用 {{}}  插值表达式来设置值
                    src="{{img}}" 这样写可以显示但是会报错

                应该使用v-bind:src 来绑定属性值
            -->
            <img v-bind:src="img" alt="切水果" @mouseover="fun2()">
        </div>
    </div>


    <hr>


</div>
</body>
</html>
<script src="./js/vue.js"></script>
<script>
    var app = new Vue({
        el: "#app",
        data: {
            msg: "hello Vue",
            img: "./2312.jpg",
            users: [
                {id: 1, name: "黄智贤", age: 23, studentID: 20240901},
                {id: 2, name: "蔡徐坤", age: 11, studentID: 20240902},
                {id: 3, name: "黄晓明", age: 28, studentID: 20240903},
                {id: 4, name: "成龙", age: 53, studentID: 20240904},
            ],
            product: {id: 1, name: "手机", price: 100},
            tag: "<a href='www.baidu.com'>百度一下</a>",
            // 一定要给一个默认值
            key_code: "1"
        },
        methods: {
            fun1(event) {
                //    这里书写点击后的逻辑
                alert("点击了测试@click");
            },
            fun2() {
                console.log("fun2测试 鼠标移上去了mouseover");
            },
            fun3() {
                console.log("fun3测试鼠标移走了 mouseout");
            },
            fun4(event) {
                let key_code = event.keyCode;
                this.key_code = key_code;

            },
            fun5(event) {
                console.log("div3上面的mouseover鼠标移入事件");
            },
            fun6() {
                alert("图片被点击了");
            },
            fun7() {
                alert("回车键被按下了");
            },
            fun8() {
                console.log("ctrl被按下了")
            }
        }
    });
</script>
```



-----

③. v-show ; v-if指令

​	作用：都是用来控制html标签  哪些显示  哪些不显示
​	语法：直接在对应标签加入v-show="true"   v-if="false"

​		例如： 

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <style>
        .img_sg {
            whith: 100px;
            height: 100px
        }

        .img_dj {
            width: 100px;
            height: 120px
        }

        .dashed_div {
            width: 200px;
            height: 200px;
            border: 10px dashed green;

        }

        .solid_div {
            width: 200px;
            height: 200px;
            border: 10px solid black;
        }

    </style>
</head>
<body>
<div id="app">
    <h1>{{ msg }}</h1>
    <!--
        v-if 指令 和 v-show指令的使用
        这两个指令都是对页面元素的隐藏与显示
        需求：不同人看到的页面不一样，比如管理员能看到所有页面元素，访客可能就看不到有一些元素

        v-if="true"   显示
        v-show="true" 显示

        区别：
            v-if 作用于标签上值为false时，会删除dom树上的这个节点（页面开销）
            v-show 只是控制当前标签的css样式。其dom树还在。
            F12查看
    -->
    <div v-if="true">hellow1</div>
    <!--    使用vue来控制-->
    <div v-if="isShow">hellow2</div>


    <!--  v-show也可以控制页面元素是否显示  -->
    <div v-show="true">hellow3</div>
    <!--    使用vue对象中的data属性-->
    <div v-show="isShow">hellow4</div>

    <br>

    <button @click="fun2">点我hellow2和hellow4显示或者隐藏</button>

    <hr>
    <hr>
    <hr>

    

</div>
</body>
</html>
<script src="js/vue.js"></script>
<script>
    new Vue({
        el: "#app", //作用范围
        data: {
            msg: "hello vue",
            isShow: "true",
            img_src: "./img/2312.jpg",
            img_style: "img_sg",
            div_css: "dashed_div"
        },
        methods: {
            fun1() {
                0
                console.log("fun1")
                this.isShow = false;
            },
            fun2() {
                console.log("fun2执行le");
                this.isShow = !this.isShow;
            },
            fun3() {
                console.log("fun3执行了");
                this.img_src = "./img/2312.jpg";
                this.img_style = "img_sg"
            },
            fun4() {
                console.log("fun4执行了");
                this.img_src = "./img/dijia.jpeg";
                this.img_style = "img_dj"
            },
            dashedLine() {
                this.div_css = "dashed_div"
            },
            solidLine() {
                this.div_css = "solid_div"
            }
        }
    });
</script>
```

​	区别：v-if操作dom	v-show 操作css
​	总结：页面元素频繁的显示隐藏 变化快 v-show	  页面元素变化慢v-if

-----

④.  v-bind 指令

​	作用：用来将html标签属性  绑定vue实例进行管理	

​	语法：绑定哪个数据直接在属性前加入   v-bind:属性名="vue管理属性值"
​	简化写法：v-bind：

​	简化之后直接一个冒号   ：

​	例如：```<img  :src="img_src" >```

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <style>
        .img_sg {
            whith: 100px;
            height: 100px
        }

        .img_dj {
            width: 100px;
            height: 120px
        }

        .dashed_div {
            width: 200px;
            height: 200px;
            border: 10px dashed green;

        }

        .solid_div {
            width: 200px;
            height: 200px;
            border: 10px solid black;
        }

    </style>
</head>
<body>
<div id="app">
    <h1>{{ msg }}</h1>
    
    <!--    v-bind 指令的使用
            v-bind指令是用来，绑定 标签上的属性的，
            将标签属性的值，交给vue实例来管理
            -->
    <!--    使用场景：鼠标移入照片上，切换照片，移除照片切换照片-->
    <!--思路：切换img的src 让其动态的指定不同照片的路径，使用v-bind来绑定属性
              鼠标移动到img图片上，和从img图片上移除，都绑定事件。来切换src照片地址
    -->

    <!--    <img src="./img/2312.jpg" alt="切水果">-->
    <!--    这里，img图片源的 src 通过v-bind：进行绑定了。
            img 图片的 class 样式 也进行了绑定， 达到一定条件，样式就改变了。
            使用了， v-bind： 的简化写法  ： 直接一个冒号就可以了
    -->
    <img :class="img_style" v-bind:src="img_src" alt="照片" @mouseover="fun3" @mouseout="fun4">


    <hr>
    <hr>
    <hr>
    <!--    只要是html标签所具有的属性，其都可以进行绑定，
        除了form 表单中 input框框中的value值，只能用 v-mode 来绑定
        -->
    <!--这里是div的样式为动态的-->
    <div :class="div_css"></div>
    <br>
    <br>
    <button @click="dashedLine">切换为虚线</button>
    <button @click="solidLine">切换为实线</button>

</div>
</body>
</html>
<script src="js/vue.js"></script>
<script>
    new Vue({
        el: "#app", //作用范围
        data: {
            msg: "hello vue",
            isShow: "true",
            img_src: "./img/2312.jpg",
            img_style: "img_sg",
            div_css: "dashed_div"
        },
        methods: {
            fun3() {
                console.log("fun3执行了");
                this.img_src = "./img/2312.jpg";
                this.img_style = "img_sg"
            },
            fun4() {
                console.log("fun4执行了");
                this.img_src = "./img/dijia.jpeg";
                this.img_style = "img_dj"
            },
            dashedLine() {
                this.div_css = "dashed_div"
            },
            solidLine() {
                this.div_css = "solid_div"
            }
        }
    });
</script>
```



⑤. v-for指令
	作用：用来对vue实例中数据进行循环遍历 到指定html标签中
	语法：直接在对应标签上使用对象数组数组对象v-for="（index,value,key）（index,item）（index,item）in vue实例遍历数据"

​     注意：建议后面跟着  :key="唯一标识" 这是为了使vue更好的确定唯一值

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <style>
        #table, #table tr, #table tr td, #table tr th {
            border: 1px solid red;
            border-collapse: collapse;
            text-align: center;
        }

        #table tr, #table tr td, #table tr th {
            width: 80px;
            height: 20px;
        }
    </style>
</head>
<body>
<div id="app">

    <h1>{{ msg }}</h1>

    <hr>
    <br>

    <!--    v-for的使用
            v-for 可以用来，遍历对象
            要想将哪个标签循环显示，就直接在这个标签上面使用 v-for

            <td v-for="" :key=""></td>

            遍历对象固定写法：v-for=" 单个值 角标 in vue实例中的属性 " :key="单个值.id"
                           例如：v-for="(index,value,key) in user" :key="value.id"

             固定写法：
                  遍历数组
                  如： v-for="index,user  in users" :key="user.id"
             注意：这里面的index是当前循环的角标 0开始，而且要写在第一个位置
    -->

    <span>做一个表格</span>

    <table id="table">
        <tr>
            <th>id</th>
            <th>姓名</th>
            <th>年龄</th>
            <th>学号</th>
            <th>成绩</th>
        </tr>
        <tr v-for="(index,user) in users " :key="user.id">
            <td>{{ index + 1 }}</td>
            <td v-text="user.name"></td>
            <td v-html="user.age"></td>
            <td>{{ user.studentID }}</td>
            <td>{{ user.fraction }}</td>
        </tr>
    </table>

    <ul>
        <li v-for="(index ,user) in users" :key="user.id">{{ user.name }}_------{{ index }}</li>
    </ul>


    <hr>
    <hr>
    <hr>
    <hr>
    <hr>

    <!--    v-mode的使用 是用来绑定 form表单中的value的属性
            v-mode 是最能直接体现 VMMV 的  双向绑定特性
            如下：
    -->

    <form>
        用户名：<input type="text" v-model="user.name"> <br>
        密码：<input type="text" v-model="user.password"><br>
        email: <input type="text" v-model="user.email"><br>
        qq: <input type="text" v-model="user.qq"><br>
        <!--        注册按钮，是用来 发送 ajax请求的 获取form表单中所有的值-->
        <input type="button" value="注册" @click="submit">
    </form>

</div>
</body>
</html>

<script src="./js/vue.js"></script>
<script>
    new Vue({
        el: "#app",
        data: {
            msg: "hello Vue",
            users: [
                {name: "张三", age: 23, studentID: 20240901, fraction: 180},
                {name: "李四", age: 24, studentID: 20240902, fraction: 100},
                {name: "万物", age: 25, studentID: 20240903, fraction: "4411"},
            ],
            user: {}
        },
        methods: {
            submit() {
                console.log(this.user);
            }
        }
    })
</script>
```





⑥.  v-model指令
	作用：用来将form表单中 绑定value 的值     value属性绑定给vue实例进行管理
	注意：最能直观体现双向绑定原理的指令
	语法：直接在对应form表单标签中加入```<input v-model="username"/>```

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <style>
        #table, #table tr, #table tr td, #table tr th {
            border: 1px solid red;
            border-collapse: collapse;
            text-align: center;
        }

        #table tr, #table tr td, #table tr th {
            width: 80px;
            height: 20px;
        }
    </style>
</head>
<body>
<div id="app">

    <h1>{{ msg }}</h1>

    <hr>
    <br>

    <!--    v-mode的使用 是用来绑定 form表单中的value的属性
            v-mode 是最能直接体现 VMMV 的  双向绑定特性
            如下：
    -->

    <form>
        用户名：<input type="text" v-model="user.name"> <br>
        密码：<input type="text" v-model="user.password"><br>
        email: <input type="text" v-model="user.email"><br>
        qq: <input type="text" v-model="user.qq"><br>
        <!--        注册按钮，是用来 发送 ajax请求的 获取form表单中所有的值-->
        <input type="button" value="注册" @click="submit">
    </form>

</div>
</body>
</html>

<script src="./js/vue.js"></script>
<script>
    new Vue({
        el: "#app",
        data: {
            msg: "hello Vue",
            users: [
                {name: "张三", age: 23, studentID: 20240901, fraction: 180},
                {name: "李四", age: 24, studentID: 20240902, fraction: 100},
                {name: "万物", age: 25, studentID: 20240903, fraction: "4411"},
            ],
            user: {}
        },
        methods: {
            submit() {
                console.log(this.user);
            }
        }
    })
</script>
```





## 4.小案例：

### 4.1备忘录小案例

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>备忘录小案例</title>
</head>
<body>
<div id="app">

<!--

    <a href="javascript:" @click="deleteByIndex(index)">删除</a>

    a标签中的 href="javascript:" 阻止了a标签的默认行为，就是加上这句话后，点击a标签，不会跳转刷新页面

-->

    <!--    <h1>{{ msg }}</h1>-->
    请输入内容：<input type="text" v-model="msg"> <input type="button" value="添加到备忘录" @click="addMemo"><br>
    <ul>
        <!--        <li>1.今天晚上吃点好吃的</li>-->
        <!--        <li>2.今天晚上一起打游戏</li>-->
        <!--        <li>3.今天晚上一起学习</li>-->
        <li v-for=" (index ,record) in lis" :key="record.id" track-by="$index">{{ index + 1 }}.{{ record }} <a href="javascript:" @click="deleteByIndex(index)">删除</a></li>

    </ul>
    <br>
    <a href="javascript:" @click="clearMemo">清空备忘录</a>
    <br>
    <h3>当前备忘录共：{{ lis.length }}条</h3>
</div>
</body>
</html>
<script src="js/vue.js"></script>
<script>
//
    new Vue({
        el: "#app",
        data: {
            msg: "",
            lis: ["今天晚上吃点好吃的", "今天晚上一起打游戏", "今天晚上一起学习"]
        },
        methods: {
            addMemo() {
                //     将input框框中的值添加到 li列表中
                if (!this.msg) {
                    alert("请输入内容！");
                    return false;
                }
                this.lis.push(this.msg);
                this.msg = "";

            },
            deleteByIndex(index){
              this.lis.splice(index, 1);
            },
            clearMemo() {
                //     清空备忘录
                this.lis.deleteData();

            }
        }
    })
</script>
```



 ## 5.练习

### 1.购物车案例

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>购物车案例</title>
    <style>
        #table {
            margin: 0 auto;
        }
        #table, #table tr, #table th, #table td {
            width: 500px;
            border: 1px solid black;
            text-align: center;
            border-collapse: collapse;
        }
    </style>
</head>
<body>
<div id="app">
    <h1>购物车案例</h1>
    编号：<input type="text">
    名称：<input type="text">
    价格：<input type="text">
    数量：<input type="text">
    <button>添加到购物车</button>


    <br>
    <br>


    <table id="table">
        <tr>
            <th>编号</th>
            <th>名称</th>
            <th>价格</th>
            <th>购买数量</th>
            <th>小计</th>
        </tr>
        <tr v-for="(index,product) in products" :key="product.id">
            <td>{{ index + 1 }}</td>
            <td>{{ product.name }}</td>
            <td>{{ product.price }}</td>
            <td><input type="button" value="+" @click="quantityIn(index)">&nbsp{{ product.number }}&nbsp<input
                    type="button" value="-" @click="quantityRt(index)"></td>

            <!--            这种计算的列，直接写计算表达式 -->
            <td>{{ (product.number * product.price).toFixed(2) }}</td>
        </tr>
        <tr>
            <td>{{ products.length + 1 }}</td>
            <td>总价格</td>
            <td colspan="3">{{ totalPrice }}</td>
        </tr>
    </table>

</div>
</body>
</html>
<script src="js/vue.js"></script>
<script>
    new Vue({
        el: "#app",
        data: {
            stock: 10,  //库存数
            products: [
                {name: "iphone8", price: 19.95, number: 1},
                {name: "meta40 pro", price: 25.22, number: 1}],
            total: 0
        },
        methods: {

            // 数量增加
            quantityIn(index) {
                if (this.products[index].number < this.stock) {
                    this.products[index].number++;
                    // 总价格变化
                    this.totalPrice;
                } else {
                    alert("最多只能买10件")
                }
            },
            // 数量减少
            quantityRt(index) {
                if (this.products[index].number > 1) {
                    this.products[index].number--;
                    // 总价格变化
                    this.totalPrice;
                } else {
                    alert("数量错误");
                }
            },

        },
        //做计算用的属性
        /*
        * 作用：
                用来在vue实例中完成相关业务计算工作
                日后在将某个数据渲染页面时  （如果该数据不是直出）需要经过业务处理之后(计算逻辑)再渲染就可以使用 computed
          好处： 提升vue运行性能主要使用computed进行计算相关处理会将第一次计算结果进行缓存，
          *     便于日后页面多次使用{{}}页面中多次使用插值表达式来取这个值
          *     使用的时候，直接写属性名totalPrice，不要写totalPrice()
        * */
        computed: {
            //计算总价格
            // 在计算值的时候，从data中取值，一定要注意 取的是字符串，一定要转为int 或者 float
            totalPrice() {
                console.log("===============methods=============");
                var totalPrice = 0;
                for (let i = 0; i < this.products.length; i++) {
                    totalPrice += this.products[i].number * this.products[i].price;
                }
                return totalPrice.toFixed(2);
            }
        },
        created() {
            this.totalPrice;
        }
    });

</script>
```



##  6.computed 属性的使用

```text
* 作用：
        用来在vue实例中完成相关业务计算工作
        日后在将某个数据渲染页面时  （如果该数据不是直出）需要经过业务处理之后(计算逻辑)再渲染就可以使用 computed
  好处： 提升vue运行性能主要使用computed进行计算相关处理会将第一次计算结果进行缓存，
  *     便于日后页面多次使用{{}}页面中多次使用插值表达式来取这个值
```

```html
-------------------------------------------------------
<td>总价格</td>
            <td colspan="3">{{ totalPrice }}</td>
            <td colspan="3">{{ totalPrice }}</td>
            <td colspan="3">{{ totalPrice }}</td>
这里面页面使用了三次{{ totalPrice }} 实际只计算1次，因为 totalPrice 的计算，放在了computed: {} 里面
实际上，只要不是直出数据渲染到页面上，就可以将计算，放在computed: {} 中。来提高运行速度
-----------------------------------------------------

//做计算用的属性
        /*
        * 作用：
                用来在vue实例中完成相关业务计算工作
                日后在将某个数据渲染页面时  （如果该数据不是直出）需要经过业务处理之后(计算逻辑)再渲染就可以使用 computed
          好处： 提升vue运行性能主要使用computed进行计算相关处理会将第一次计算结果进行缓存，
          *     便于日后页面多次使用{{}}页面中多次使用插值表达式来取这个值
        * */
 new Vue({
        	el: "#app",
        	data: {
			
			},
			methods:{

			},
        	computed: {
            	//计算总价格
            	// 在计算值的时候，从data中取值，一定要注意 取的是字符串，一定要转为int 或者 float
            	totalPrice() {
                	console.log("===============methods=============");
                	var totalPrice = 0;
                	for (let i = 0; i < this.products.length; i++) {
                        totalPrice += this.products[i].number * this.products[i].price;
                	}
                	return totalPrice.toFixed(2);
            	}
        	}
      	});
```



## 7.事件修饰符

>7.1    .stop
>    		作用：用来阻止事件冒泡   用来阻止事件继续问外层传递（前提是内外层的事件，是同种类型的事件）
>7.2   .prevent
>    		作用：用来阻止标签的默认行为
>7.3	.self
>    		作用：只监所自身标签触发的事件，一般用在父级元素上面，只关注父元素本身事件，子元素就算触发了它的事件，父元素也会忽略
>7.4	.once
>    		作用：该事件只触发一次



### 7.1 阻止标签的默认行为发生

```html
<!--    阻止 标签的默认行行为
            方法1： 在事件上使用prevent修饰符来，去除a标签的默认行为
            方法2：在href上面写 <a href="javaScript:" @click="a">a标签</a>
    -->
    <br>
    <hr>

    <a href="" @click.prevent="a">a标签</a>
    <br>
    <a href="javaScript:" @click="a">a标签</a>
```



```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>

    <style>
        #div1 {
            //border: 3px solid red;
            background: red;
            width: 500px;
            height: 500px;
        }

        #div2 {
            //border: 3px solid orchid;
            background: yellow;
            width: 300px;
            height: 300px;
            margin-left: 100px;
            float: left;
            margin-top: 100px;
        }

        #div3 {
            //border: 3px solid green;
            background: blue;
            width: 100px;
            height: 100px;
            margin-left: 100px;
            float: left;
            margin-top: 100px;
        }

    </style>
</head>
<body>

<!--
    .stop
        作用：用来阻止事件冒泡   用来阻止事件继续问外层传递（前提是内外层的事件，是同种类型的事件）
    .prevent
        作用：用来阻止标签的默认行为
    .self
        作用：只监所自身标签触发的事件，一般用在父级元素上面，只关注父元素本身的事件，子元素就算触发了它的事件，父元素也会忽略
    .once
        作用：该事件只触发一次
-->
<div id="app">
    <h1>{{ msg1 }}</h1>
    <!--    事件冒泡：
                前提是，外层元素标签绑定的事件，和内层标签绑定的是同一种事件，
                当内层子标签，触发了它上面的事件，
                其外层父标签也会自动除法事件。
                这就是事件冒泡

                解决办法： 不想让事件往外层冒泡，可以直接在内层标签元素的事件上面，加上事件修饰符.stop 就是 用来阻止其事件往外层传递
    -->
    <div id="div1" @click="div1click">
        <div id="div2" @click.stop="div2click">
            <div id="div3" @click.stop="div3click">

            </div>
        </div>
    </div>


    <!--    阻止 标签的默认行行为
            方法1： 在事件上使用prevent修饰符来，去除a标签的默认行为
            方法2：在href上面写 <a href="javaScript:" @click="a">a标签</a>
    -->
    <br>
    <hr>

    <a href="" @click.prevent="a">a标签</a>
    <br>
    <a href="javaScript:" @click="a">a标签</a>


</div>
</body>
</html>
<script src="js/vue.js"></script>
<script>
    new Vue({
        el: "#app",
        data: {
            msg1: "事件冒泡"
        },
        methods: {
            div1click() {
                console.log("div1被点击了");
            },
            div2click() {
                console.log("div2被点击了");
            },
            div3click() {
                console.log("div3被点击了");
            },
            a() {
                console.log("a标签被点击了，执行了该方法，默认的行为href跳转失效了");
            }

        },
        computed: {}
    })
</script>
```



## 8.按键修饰符（和键盘事件连用“keydown”“keyup”"keypress""）

**Vue** **允许为** **v-on** **在监听键盘事件时添加按键修饰符 **

**就是把我们常用的键盘按键，给做成了属性，我们在使用“keydown”“keyup”"keypress""事件的时候，可以直接在：“事件后面.属性”** 

>
>
>.enter
>
>.tab
>
>.delete (捕获"删除"和 "退格"键)
>
>.esc
>
>.space
>
>.up
>
>.down
>
>.left
>
>.right
>
>.ctrl
>
>.alt
>
>.shift
>
>.meta

```html
<!DOCTYPE html>
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8"/>
    <title>v-on 按钮修饰符</title>

</head>
<body>
<div id="app">
    <input type="text" @keydown.enter="fun1">
</div>
<script src="js/vue.js"></script>
<script>
    new Vue({
        el: '#app', //表示当前vue对象接管了 div 区域
        methods: {
            fun1: function () {
                alert("你按了回车");
            }
        }
    })
</script>
</body>
</html>
```





子组件事件传递，
子组件，调用父组件中的，函数。
可以传一个参数。
反过来看，就明白了？变相子组件，传数据给父组件



{% endraw %}



---


----

© 版权声明

<escape>

<div>
    <h3 align="center"  style="color: brown;" >版权声明</h3>
    <table>
   		<tr>
    		<ol>
				<li>本网站名称：𝚲𝚳𝚲</li>
				<li>𝚲𝚳𝚲提供的资源仅供您个人用于非商业性目的。</li>
				<li>本站文章部分内容可能来源于网络，仅供大家学习与参考，如有侵权，请联系我进行删除处理。</li>
				<li>本站一切资源不代表本站立场，并不代表本站赞同其观点和对其真实性负责。</li>
        		<li>本站一律禁止以任何方式发布或转载任何违法的相关信息，访客发现请举报</li> 
        		<li>本站资源大多存储在云盘，如发现链接失效，请联系我，我会第一时间更新。</li>
        		<li>本站强烈打击盗版/破解等有损他人权益和违法作为，请支持正版！</li>  
			</ol>
		</tr>
	</table>
</div>







</escape>

----



